const add = (num1, num2) => {
  return num1 + num2
}

const subtract = (num1, num2) => {
  return num1 - num2
}

const double = (num) => {
  return num * 2
}

module.exports ={
  add,
  subtract,
  double
}


